import type { NextPage } from "next";

import { HomePage } from "@modules/landingPages";

const Home: NextPage = () => {
  return (
    <div>
      <HomePage />
    </div>
  );
};

export default Home;
